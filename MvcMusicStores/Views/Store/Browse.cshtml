@model IEnumerable<MvcMusicStores.Models.Album>
@{
    ViewBag.Title = "Browse";
}
@*<h2>Browsing Genre: @Model.GenreId</h2>
*@<ul>
    @foreach (var album in Model)
    {
        <li>
             @Html.ActionLink(album.Title,
"Details", new { id = album.AlbumId })
        </li>
    }
</ul>